Setting up the project
	> Ensure the latest/stable version of python & pip are installed.
	> Install dependencies by navigating to the file directory and running ` pip install -r requirements.txt `. 
	> Run the project by navigating to the project directory and running the main.py file.
	
Credits
